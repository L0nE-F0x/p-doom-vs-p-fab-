{
  "name": "pdoom-vs-pfab",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "gate": "node scripts/check-claims.mjs",
    "build": "npm run gate && npm test && cp web/balance.html web/index.html",
    "fetch": "node scripts/dev-fetch.mjs"
  }
}
